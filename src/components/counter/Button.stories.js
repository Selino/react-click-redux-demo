import React from "react"
import { Button } from "react-bootstrap"

export default {
  title: "Button",
  component: Button,
}

export const Primary = (args) => <Button {...args}>Poop</Button>
// export const Success = (args) => <MiniBar {...args} />
